/**
 * this interface specifies the structure of the expected data that will come from the api
 * concerning the change, current price etc. .
 */
export interface DataDetail {
  c: number;
  d: number;
  dp: number;
  h: number;
  l: number;
  o: number;
  pc: number;
}
